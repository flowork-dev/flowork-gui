#######################################################################
# WEBSITE https://flowork.cloud
# File NAME : C:\FLOWORK\flowork-gui\template\web\.env.production
# (MODIFIED BY GEMINI - Sesuai Kontrak Baru & .env backend)
#######################################################################

# (English Hardcode) This is the single, public entrypoint for both
# (English Hardcode) API (HTTPS) and Socket.IO (WSS).
# (English Hardcode) It MUST match your backend's GATEWAY_URL/.env.
VITE_GATEWAY_URL=https://socket.flowork.cloud

# --- (KODE ASLI DI-KOMEN - SESUAI RULE #2) ---
# (English Hardcode) Comments below are deprecated, VITE_GATEWAY_URL is the single source of truth.
#
# (PERBAIKAN KRITIS OLEH GEMINI):
# Variabel ini HARUS DIHAPUS atau dikomentari.
# ... (komentar lama tidak relevan lagi) ...
#
# URL untuk API Gateway Anda (tempat login dan daftar engine)
# (PERBAIKAN SAYA - GEMINI): Baris ini HARUS di-uncomment dan diisi
#VITE_GATEWAY_URL=https://api.flowork.cloud  # <-- (PERBAIKAN) Ganti dengan URL tunnel Gateway Anda
#
# URL untuk koneksi WebSocket ke Flowork Engine lokal ANDA
# (Ini sudah benar dari Cloudflare Tunnel)
# (English Hardcode) This specific variable is no longer used by the new socket.js
#VITE_LOCAL_ENGINE_WS_URL=wss://socket.flowork.cloud
# --- (AKHIR MODIFIKASI) ---